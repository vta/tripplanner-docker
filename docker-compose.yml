version: "2"

services:
  otp:
    build: ./otp
    image: scvta/otp
    restart: always
#    networks:
#      - backend
  apache:
    build: ./apache
    image: scvta/otp-apache
    restart: always
#    networks:
#      - frontend
#      - backend
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - otp
    links:
      - otp

